import Link from "next/link";
import Container from "@/components/ui/Container";
import SeoDisclaimer from "@/components/SeoDisclaimer";
import type {ProjectService} from "@/lib/data/pricing-services";
export default function ContentRefreshOffer({service}:{service?:ProjectService}){if(!service)return null;return <section id="content-refresh" className="py-14"><Container><div className="rounded-3xl border border-border bg-surface p-7 sm:p-10"><p className="text-sm font-semibold uppercase tracking-widest text-secondary">Improve the website you already have</p><h2 className="mt-3 font-display text-3xl">{service.name}</h2><p className="mt-4 text-2xl font-semibold">Starting at ${service.oneTimePrice?.toLocaleString('en-US')}</p><p className="mt-2 font-semibold">Up to {service.includedPages} Pages Included · Additional Pages: ${service.additionalPagePrice}/page · Maximum 10 Pages</p><p className="mt-5 max-w-3xl leading-relaxed">{service.shortDescription}</p><ul className="my-6 grid gap-3 sm:grid-cols-2">{['Headlines, messaging and calls-to-action','Clarity, readability and consistent wording','Service/product descriptions','Grammar, proofreading and outdated wording','Content organization and flow','Basic content-level SEO where appropriate'].map(x=><li key={x}>✓ {x}</li>)}</ul><p className="text-sm leading-relaxed text-muted">This is a refresh of existing content. Redesign, development, major layout changes, technical SEO, link building, new branding or logos, extensive research, an entirely new website’s copy, photography and videography require a separate quote. Final pricing may increase for substantial rewriting, research, unusual complexity or work outside the standard scope.</p><SeoDisclaimer/><Link href="/content-refresh" className="mt-6 inline-block rounded-full bg-cta px-6 py-3 font-semibold text-cta-foreground">Refresh My Website</Link></div></Container></section>;}
